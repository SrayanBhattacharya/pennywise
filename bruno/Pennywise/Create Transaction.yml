info:
  name: Create Transaction
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/transactions"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "categoryId": "844bf94a-092c-4f48-9b5e-00474f632447",
        "amount": 185.00,
        "title": "Cab Ride",
        "merchantName": "Uber",
        "transactionDate": "2026-07-11",
        "notes": "Office commute",
        "recurring": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
