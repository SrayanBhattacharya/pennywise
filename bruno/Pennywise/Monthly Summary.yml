info:
  name: Monthly Summary
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/dashboard/monthly-summary"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
