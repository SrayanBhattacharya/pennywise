info:
  name: Unlock Statement
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/statements/9ecce9e9-7a1f-4b31-adb0-e3643d9142bb/unlock"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
          "password": "71895100904"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
