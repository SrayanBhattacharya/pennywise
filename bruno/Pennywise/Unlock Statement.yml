info:
  name: Unlock Statement
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/statements/e6c2b594-ac88-452b-bb70-9c040f066095/unlock"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
          "password": "71895100904"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
