info:
  name: Statement Upload
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/statements/upload"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/srayan/Projects/pennywise/samples/bank-statements/sbi/statement.xlsx
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
