info:
  name: DashboardCategory
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/dashboard/category-breakdown"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
